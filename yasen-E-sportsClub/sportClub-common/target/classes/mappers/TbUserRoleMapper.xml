<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yasen.mappers.TbUserRoleMapper">

	<!--实体映射-->
	<resultMap id="base_result_map" type="com.yasen.entity.pojo.TbUserRole">
		<!---->
		<result column="id" property="id"  />
		<!---->
		<result column="user_id" property="userId"  />
		<!---->
		<result column="role_id" property="roleId"  />
	</resultMap>


	<!-- 通用查询结果列-->
	<sql id="base_column_list">
		 t.id,t.user_id,t.role_id
	</sql>

	<sql id="base_condition_filed">
			<if test="query.id != null">
				 and  t.id = #{query.id}
			</if>
			<if test="query.userId != null">
				 and  t.user_id = #{query.userId}
			</if>
			<if test="query.roleId != null">
				 and  t.role_id = #{query.roleId}
			</if>
	</sql>

	<!-- 通用查询条件列-->
	<sql id="query_condition">
	 <where>
			<include refid="base_condition_filed" />
	 </where>
	</sql>

	<!-- 查询集合-->
	<select id="selectList" resultMap="base_result_map" >
		 SELECT <include refid="base_column_list" /> FROM tb_user_role t <include refid="query_condition" />
		 <if test="query.orderBy!=null">
			 order by ${query.orderBy}
		 </if>
		 <if test="query.simplePage!=null">
			 limit #{query.simplePage.start},#{query.simplePage.end}
		 </if>
	</select>

	<!-- 查询数量-->
	<select id="selectCount" resultType="java.lang.Integer" >
		 SELECT count(1) FROM tb_user_role t <include refid="query_condition" />
	</select>

	<!-- 插入 （匹配有值的字段）-->
	<insert id="insert" parameterType="com.yasen.entity.pojo.TbUserRole">
		 INSERT INTO tb_user_role
		 <trim prefix="(" suffix=")" suffixOverrides="," >
			<if test="bean.id != null">
				 id,
			</if>
			<if test="bean.userId != null">
				 user_id,
			</if>
			<if test="bean.roleId != null">
				 role_id,
			</if>
		 </trim>
		 <trim prefix="values (" suffix=")" suffixOverrides="," >
			<if test="bean.id!=null">
				 #{bean.id},
			</if>
			<if test="bean.userId!=null">
				 #{bean.userId},
			</if>
			<if test="bean.roleId!=null">
				 #{bean.roleId},
			</if>
		 </trim>
	</insert>

	<!-- 插入或者更新 （匹配有值的字段）-->
	<insert id="insertOrUpdate" parameterType="com.yasen.entity.pojo.TbUserRole">
		 INSERT INTO tb_user_role
		 <trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="bean.id != null">
				 id,
			</if>
			<if test="bean.userId != null">
				 user_id,
			</if>
			<if test="bean.roleId != null">
				 role_id,
			</if>
		 </trim>
		 <trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="bean.id!=null">
				 #{bean.id},
			</if>
			<if test="bean.userId!=null">
				 #{bean.userId},
			</if>
			<if test="bean.roleId!=null">
				 #{bean.roleId},
			</if>
		 </trim>
		 on DUPLICATE key update 
		 <trim prefix="" suffix="" suffixOverrides=",">
			<if test="bean.id!=null">
				 id = VALUES(id),
			</if>
			<if test="bean.userId!=null">
				 user_id = VALUES(user_id),
			</if>
			<if test="bean.roleId!=null">
				 role_id = VALUES(role_id),
			</if>
		 </trim>
	</insert>

	<!-- 添加 （批量插入）-->
	<insert id="insertBatch" parameterType="com.yasen.entity.pojo.TbUserRole">
		 INSERT INTO tb_user_role(
			 id,
			 user_id,
			 role_id
		 )values
		 <foreach collection="list" item="item" separator=",">
			 (
				 #{item.id},
				 #{item.userId},
				 #{item.roleId}
			 )
		 </foreach>
	</insert>

	<!-- 批量新增修改 （批量插入）-->
	<insert id="insertOrUpdateBatch" parameterType="com.yasen.entity.pojo.TbUserRole">
		 INSERT INTO tb_user_role(
			 id,
			 user_id,
			 role_id
		 )values
		 <foreach collection="list" item="item" separator=",">
			 (
				 #{item.id},
				 #{item.userId},
				 #{item.roleId}
			 )
		 </foreach>
			on DUPLICATE key update 
			id = VALUES(id),
			user_id = VALUES(user_id),
			role_id = VALUES(role_id)
	</insert>

	<!--多条件修改-->
	<update id="updateByParam" parameterType="com.yasen.entity.query.TbUserRoleQuery">
		 UPDATE tb_user_role t
 		 <set> 
			<if test="bean.id != null">
				 id = #{bean.id},
			</if>
			<if test="bean.userId != null">
				 user_id = #{bean.userId},
			</if>
			<if test="bean.roleId != null">
				 role_id = #{bean.roleId},
			</if>
 		 </set>
 		 <include refid="query_condition" />
	</update>

	<!--多条件删除-->
	<delete id="deleteByParam">
		 delete t from tb_user_role t
 		 <include refid="query_condition" />
	</delete>

	<!-- 根据Id修改-->
	<update id="updateById" parameterType="com.yasen.entity.pojo.TbUserRole">
		 UPDATE tb_user_role
 		 <set> 
			<if test="bean.userId != null">
				 user_id = #{bean.userId},
			</if>
			<if test="bean.roleId != null">
				 role_id = #{bean.roleId},
			</if>
 		 </set>
 		 where id=#{id}
	</update>

	<!-- 根据Id删除-->
	<delete id="deleteById">
		delete from tb_user_role where id=#{id}
	</delete>

	<!-- 根据PrimaryKey获取对象-->
	<select id="selectById" resultMap="base_result_map" >
		select <include refid="base_column_list" /> from tb_user_role t where id=#{id}
	</select>

</mapper>